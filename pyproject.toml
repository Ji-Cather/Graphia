[project]
name = "llmggen"
version = "0.1.0"
description = "A demo library"
authors = [{name = "Alice", email = "alice@example.com"}]
license = {text = "MIT"}
requires-python = ">=3.7"

[tool.setuptools]
packages = ["llmggen"]
package-dir = {llmggen = "src"}